[project]
name = "galaxy-station-bot"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
    "aiogram>=3.20.0",
    "python-dotenv>=1.1.0",
    "fastapi>=0.115.0",
]

[tool.vercel]
entrypoint = "app.py"
